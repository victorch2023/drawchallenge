# Variables para Vercel (proxy opción B)
# Opcional si la API key se envía desde el panel de la app:
# GEMINI_API_KEY=

# Orígenes permitidos (CORS) — incluye tu GitHub Pages
ALLOWED_ORIGINS=https://victorch2023.github.io,http://localhost:8080,http://localhost:3000
